// vectors declaration (many examples)
#include <bits/stdc++.h>
using namespace std;
int main()
{
  // vector<int> v;
  // v.push_back(1); // pushes 1 to the dynamic array v
  // v.emplace_back(2); // this alsopushes 2 to the dynamic array v , now -> {1,2}

  // //to define a vector or dynamic array that can hold , pairs of ints
  // vector<pair<int,int>> vec;
  // vec.push_back({1,2});
  // vec.emplace_back(3,4); // this is the difference between the two.

  // to create a pre-initialized vector with five 100 in the DA.
  //  vector<int> v(5,100);
  //  for(auto e:v)
  //  {
  //    cout<<e;
  //  }

  // //creates 5 elements e with value 0.
  // vector<int> v1(5);
  // for(auto e:v1)
  // {
  //   cout<<e;
  // }

  // vector<int>v2(5,20);
  // for(int e:v2)
  // {
  //   cout<<e<<" ";
  // }
  // cout<<endl;

  // copy of v2 is sent to v3.

  // vector<int>v3(v2);
  // for(int e:v3)
  // {
  //   cout<<e<<" ";
  // }
}