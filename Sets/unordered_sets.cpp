#include<bits/stdc++.h>
#include<iostream>
using namespace std;
int main()
{
  unordered_set<int>st;
  /*
  lower_bound() and upper_bound() functions wont work ,
  because , they are used to access the indexes ,
  And in an unordered situation , you can get the 
  exact index.

  except the above fxns , rest all functions are same

  has a better complexity than set in most cases , except when
  collision happens.
  */
}